# BACKLOG

Bu dosya iki tür maddeyi tutar:
1. **Migration/kod backlog'u** — 01-09 fazlarında "tespit edildi ama eklenmedi" notu düşülen
   ve bu oturumda (kodlama fazı başlangıcı) `migrations/0002_accumulated_requirements.sql`
   ile işlenen maddelerin kaydı.
2. **Pilot hata kayıtları** — 08_Test_Pilot.md §4 formatı: tarih, senaryo no, gözlem,
   önerilen düzeltme, öncelik. Pilot başlamadan bu bölüm boştur.

## A. Migration Backlog (kaynak fazlar → durum)

| # | Madde | Kaynak | Durum |
|---|-------|--------|-------|
| 1 | `appointments.idempotency_key` + `UNIQUE(tenant_id, idempotency_key)` | 03_Backend_API.md §7 | ✅ 0002'de işlendi |
| 2 | `message_log.idempotency_key` + `UNIQUE(tenant_id, idempotency_key)` | 03_Backend_API.md §7, 04 §5.c | ✅ 0002'de işlendi |
| 3 | `conversation_states` tablosu — `GET/PATCH /conversation-state?customer_id=` için n8n'in oturum adımı sorgusu/güncellemesi | 04_n8n_Workflows.md §2, §7 | ✅ Tablo 0002'de, `GET` PHASE_12'de kodlandı; **`PATCH` (yazma) PHASE_13'te eklendi** (`ConversationStateRepository::upsert`, n8n'in state'i ilerletmesi için — PHASE_12'de yalnızca okuma vardı, 04§2'nin varsaydığı yazma yolu eksikti), uçtan uca test edildi |
| 4 | `tenants.reminder_hours_before`, `tenants.pending_ttl_minutes` | 04_n8n_Workflows.md §7 | ✅ 0002'de işlendi |
| 5 | `GET /internal/appointments-due-for-reminder`, `GET /internal/appointments-expired-pending` (servis rolü, tüm tenant tarama) | 04_n8n_Workflows.md §7 | ✅ Bu oturumda kodlandı (`InternalScanController` + `AppointmentScanRepository`, `pg_try_advisory_lock` ile — madde 15'i de kapsar) |
| 6 | `tenants.whatsapp_status CHECK IN ('pending','connected','disconnected')` | 05_WhatsApp_Integration.md §7 | ✅ 0002'de işlendi (`waba_id`/`access_token_encrypted` zaten 02'de mevcuttu) |
| 7 | `POST /internal/whatsapp/send`, `POST /internal/whatsapp/templates/sync` endpoint'leri | 05_WhatsApp_Integration.md §7 | ✅ Tamamı doğrulandı. PHASE_30: `type=text` gerçek teslimat. **PHASE_32: şablon dili sabitlemesi düzeltildi** (`migrations/0005` — `message_templates.language`, sync Meta'dan dili saklar, send istek>DB>tr önceliğiyle kullanır) ve **ilk gerçek `type=template` teslimatı yapıldı** (`hello_world`/`en_US`, gerçek `wamid` ile `sent`) |
| 8 | `message_log.meta_error_code text` | 05_WhatsApp_Integration.md §7 | ✅ 0002'de işlendi |
| 9 | `POST /settings/logo` (statik dosya yükleme) | 06_Admin_Panel.md §10 | ✅ Bu oturumda kodlandı (`SettingsController::uploadLogo`, owner/manager rolü zorunlu, uçtan uca test edildi) |
| 10 | Müşteri silme/anonimleştirme (KVKK/GDPR) endpoint'i | 06_Admin_Panel.md §10, 09_SaaS_Deployment.md §6 | ✅ Bu oturumda kodlandı (`DELETE /customers/{id}` → anonimleştirme, gerçek satır silinmez; owner/manager rolü zorunlu, uçtan uca test edildi) |
| 11 | `POST /ai/respond` için tenant bazlı özel rate limit eşiği → `ai_settings.rate_limit_per_minute` | 07_AI_Module.md §7 | ✅ Bu oturumda kodlandı (`RateLimiter` + `RedisClient`, sabit dakikalık pencere/INCR+EXPIRE; Redis erişilemezse fail-open — `AiRespondController`'a entegre, `source: fallback:rate_limited`); gerçek Redis'e karşı uçtan uca test edildi (madde 16) |
| 12 | `plans` tablosu + `tenants.plan_id` | 09_SaaS_Deployment.md §6 | ✅ 0002'de işlendi |
| 13 | `tenants.subscription_status/billing_customer_id/trial_ends_at` | 09_SaaS_Deployment.md §6 | ✅ 0002'de işlendi |
| 14 | Platform admin rolü + ayrı route grubu (`platform_admins` tablosu) | 09_SaaS_Deployment.md §5, §6 | ✅ Bu oturumda kodlandı (`PlatformAdminController`/`PlatformTenantController`, `PlatformAdminAuthMiddleware` — panel JWT'sinden ayrı `type: platform_admin` claim'i; `POST /platform/auth/login`, `GET /platform/tenants`, `PATCH /platform/tenants/{id}`); uçtan uca test edildi (login, cross-token reddi, tenant suspend) |
| 15 | n8n cron mutex'i — Postgres advisory lock | 09_SaaS_Deployment.md §6 | ✅ Bu oturumda madde 5 ile birlikte kodlandı (`AppointmentScanRepository::withAdvisoryLock`); n8n workflow tarafı kod fazında ele alınmadı |
| 16 | Redis (rate limit sayaçları + n8n queue mode deposu) | 09_SaaS_Deployment.md §2, §6 | ✅ Kapandı. Rate limit sayaçları: portable Windows Redis + bağımlılıksız RESP istemcisi (`RedisClient`), bkz. madde 11. **n8n queue mode: PHASE_32 kararı — tek instance yeterli, gerçek ölçekleme ihtiyacına kadar kurulmayacak;** geçiş reçetesi (Redis kuyruk deposu, `N8N_EXECUTIONS_MODE=queue`, worker env'leri, SQLite→Postgres, advisory lock'ların çift gönderimi zaten engellediği) `n8n/README.md` "Queue mode" bölümünde |
| 17 | Panelde WhatsApp mesajlaşma tier uyarı alanı | 09_SaaS_Deployment.md §2, §6 | ✅ PHASE_32'de kodlandı: `MetaGraphClient::getPhoneNumberHealth` + `GET /settings/whatsapp/health` (panel JWT; Meta'ya ulaşılamazsa `available:false` ile 200 — ayar sayfası kırılmaz) + `/panel/settings/whatsapp`'ta "Mesajlaşma Limiti ve Kalite" kartı (kalite rozeti, tier etiketi, TIER_50/250'de kampanya uyarısı). Gerçek WABA verisiyle tarayıcıda doğrulandı (GREEN, TIER_250) |
| 18 | `BACKLOG.md`, `PROJECT_MEMORY.md`, `README.md` | 09_SaaS_Deployment.md §6 | ✅ Bu oturumda oluşturuldu |
| 19 | `GET/POST /webhook/whatsapp` — Meta'dan gelen mesajları karşılayan webhook (abonelik doğrulama + imza doğrulama + ham kayıt + tenant çözümü) | 05_WhatsApp_Integration.md §2 | ✅ Bu oturumda kodlandı (`WhatsAppWebhookController`, `WebhookEventRepository`; geçersiz imza → 403 kayıtsız, geçerli imza + tenant bulunamadı → 200 + `tenant_id` NULL kayıt — bkz. CHANGELOG PHASE_12); uçtan uca test edildi, gerçek Meta kimlik bilgisi yok |
| 20 | `N8nNotifier` — Backend'in tenant çözdüğü gelen mesajı n8n'e iletmesi (`N8N_INCOMING_WEBHOOK_URL`, HMAC imzalı, best-effort) | 04_n8n_Workflows.md §1 (PHASE_12'nin Meta→Backend pivotu sonrası ortaya çıkan gereksinim) | ✅ PHASE_13'te kodlandı ve mock n8n receiver'a karşı uçtan uca test edildi (bkz. CHANGELOG PHASE_13) |
| 21 | n8n workflow JSON export'ları (`n8n/01_incoming_whatsapp_message.json`, `02_reminder_scan.json`, `03_pending_ttl_scan.json`) | 04_n8n_Workflows.md tamamı | ✅ PHASE_13'te yazıldı; **PHASE_14'te gerçek bir n8n instance'ına (v2.28.6) import edilip uçtan uca çalıştırılarak doğrulandı** — Webhook/If/Code/Switch/HTTP Request zinciri gerçek Backend + Postgres'e karşı başarıyla yürütüldü. İki gerçek n8n davranışı düzeltildi: `$json.body` `rawBody:true` ile bile parse objesi (ham veri `$input.item.binary.data.data`'dan okunmalı) ve `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` gerekliliği — bkz. `n8n/README.md`, CHANGELOG PHASE_14 |
| 22 | Gerçek in-place `PATCH /appointments/{id}/reschedule` ucu | 04_n8n_Workflows.md §4 ("eskiyi cancel etmez, time_range günceller") | ✅ PHASE_27'de kodlandı (`AppointmentRepository::reschedule`, `AppointmentController::reschedule` — `23P01` → `409 slot_taken`, terminal/yok → `422`/`404`); gerçek Postgres'e karşı uçtan uca test edildi. **PHASE_32: n8n tarafı da tek çağrıya indirildi** — `Is Reschedule (slot)?` + `Patch Reschedule Appointment` node'ları eklendi, eski cancel+yeni-appointment deseni kaldırıldı; canlı n8n'de imzalı webhook simülasyonuyla uçtan uca doğrulandı (aynı id yerinde taşındı, state idle, bilgi metni gerçek `sent`). Ayrıca slot `start_time`'ına sabit `+03:00` ofseti eklendi (testte yakalanan 1 saatlik kayma — `n8n/README.md` madde 1 ve 3 güncellendi) |
| 23 | Panel PHASE_15'te kodlanan uçlar: `GET/PATCH /settings`, `GET/PUT /staff/{id}/schedule`, `POST/DELETE /staff/{id}/holidays`, `GET/PUT /staff/{id}/services`, `PATCH /appointments/{id}/complete\|no-show` | 06_Admin_Panel.md §4, §5, §8 | ✅ PHASE_15'te kodlandı + tarayıcıda uçtan uca test edildi (bkz. CHANGELOG PHASE_15) |
| 24 | Panel kalan sayfalar: FullCalendar takvim görünümü, `/customers`, `/messages/templates\|campaigns\|log`, `/reports`, `/settings/business`, `/settings/ai`, platform admin UI, panelden randevu oluşturma | 06_Admin_Panel.md §1, §4, §6, §7, §9 | ✅ Tamamlandı. PHASE_16: FullCalendar takvim, `/customers`, `/messages/log`, `/settings/business`. PHASE_17: `/messages/templates` (+`GET /messages/templates` ucu + panel JWT sync sarmalayıcısı), panelden randevu oluşturma (409 slot_taken alternatif slot akışıyla), `/messages/campaigns` (+campaigns CRUD uçları — gönderim workflow'u YOK, yalnızca taslak/planlama), `/settings/ai` (+`GET/PATCH /settings/ai`). **PHASE_18: `/reports` (Chart.js üç grafik, 06§9 — yeni backend ucu yok, mevcut liste uçları panelde agregre) + platform admin UI (`/platform/login` + `/platform`, ayrı `platform_jwt` anahtarı, tenant suspend/activate).** Panelde yarım/eksik ekran kalmadı. |
| 25 | `/settings/whatsapp` "Yeniden Bağlan" butonu placeholder — Embedded Signup (05§1) gerçek Meta App ID/config gerektiriyor | 05_WhatsApp_Integration.md §1, 06 §8 | 🟡 PHASE_28'de eski portföydeki WABA `131031 Business Account locked` hatasıyla kalıcı kilitlendi (Meta hesap düzeyi, kod hatası değil — kullanıcı itiraz etti ama çözülemedi). **PHASE_30'da yeni bir portföy/App/WABA'ya geçildi** (`Test Berber 2` / `BerberApp` / WABA `1359511886136495`, phone_number_id `1147876331751351`) — kalıcı bir System User token'ıyla (süresiz, `whatsapp_business_management`+`whatsapp_business_messaging`) dev tenant'a bağlandı ve **gerçek bir mesaj gerçek bir telefona teslim edildi** (uçtan uca kanıtlandı). **PHASE_31'de webhook (inbound) de bağlandı** (ngrok tüneli + Meta callback URL/verify token + `subscribed_apps` düzeltmesi, bkz. madde 29) — gerçek bir gelen mesaj n8n üzerinden menüye kadar uçtan uca doğrulandı. **PHASE_32: Embedded Signup panele kodlandı** — `POST /settings/whatsapp/connect` (code→token exchange→`subscribed_apps` (m.29 otomatik)→şifreli kayıt, UNIQUE ihlalinde 409), panelde FB SDK + `FB.login(config_id)` + popup message event yakalama, `.env`'e `META_APP_ID`/`META_ES_CONFIG_ID`. Sunucu yolu gerçek Meta hatalarıyla test edildi (sahte code → 502 `step:exchange_code`). **Tek kalan kullanıcı adımı:** Meta Dashboard > Facebook Login for Business > Configurations'tan bir config ID oluşturup `.env META_ES_CONFIG_ID`'ye yazmak — sonrasında popup akışı gerçek kullanıcıyla denenebilir |
| 29 | `GET/POST /webhook/whatsapp` — Meta'nın yeni WABA (PHASE_30) için inbound webhook'unun gerçek trafikle uçtan uca doğrulanması (ngrok tüneli + Meta App webhook config + n8n) | 05_WhatsApp_Integration.md §2, 04_n8n_Workflows.md §1 | ✅ PHASE_31'de tamamlandı. ngrok kuruldu (winget paketi çok eskiydi — `ERR_NGROK_121` — güncel binary elle indirildi), sabit ücretsiz alan adı `provider-dislodge-bounce.ngrok-free.dev` → `localhost:8000`. `.env`'de o zamana kadar boş olan `META_APP_SECRET` Meta Dashboard'dan alınıp dolduruldu (webhook POST imza doğrulaması bu olmadan hep çöküyordu). Meta App'te callback URL + verify token kaydedildi (yeşil tik). **Kritik bulgu:** WABA, Business Settings > System User ile bağlandığı için (Embedded Signup değil) app'e hiç `subscribed_apps` olarak abone değildi — yalnızca Meta'nın varsayılan `WA DevX Webhook Events 1P App`'ine abone görünüyordu; `GET /{waba-id}/subscribed_apps` ile teşhis edilip `POST /{waba-id}/subscribed_apps` (aynı System User token'ıyla) ile düzeltildi. n8n yeniden başlatıldı, kullanıcının gerçek telefonundan attığı mesaj **Meta → ngrok → backend → n8n → gerçek WhatsApp interactive menü yanıtı** olarak uçtan uca doğrulandı (kullanıcı telefonunda gördü). Bu düzeltme Embedded Signup panele kodlanırken (madde 25) otomatik yapılmalı, aksi halde her yeni WABA bağlantısında aynı sessiz arıza tekrar eder. **PHASE_32: yapıldı** — `POST /settings/whatsapp/connect`, token exchange'den hemen sonra `MetaGraphClient::subscribeApp` çağırıyor, başarısız olursa bağlantı kaydedilmeden 502 `step:subscribed_apps` döner |
| 26 | Kampanya **gönderim** mekanizması — `scheduled_at` gelince `target_filter`'a uyan müşterilere şablon gönderen n8n cron workflow'u + `status='sent'` geçişi | 06_Admin_Panel.md §7 (PHASE_17'de CRUD kodlandı, gönderen taraf yok) | ✅ PHASE_23'te kodlandı (`CampaignScanRepository`/`CampaignScanController`, `GET /internal/campaigns-due-for-send` — `migrations/0004` ile `campaigns.sent_at` eklendi; `n8n/04_campaign_send.json` reminder_scan desenini izliyor). Tarama, kampanyayı atomik `UPDATE...RETURNING` ile 'sent'e claim edip aynı sorguda `target_filter`'a uyan alıcı listesini (müşteri başına bir satır) döner; `idempotency_key = <campaign_id>_<customer_id>` ile `/internal/whatsapp/send`'e iletilir. Gerçek backend + Postgres'e karşı uçtan uca test edildi (bkz. CHANGELOG PHASE_23) |
| 27 | `POST /ai/respond` — AI yanıt üretimi (Anthropic tool-use, sistem promptu + guardrail'ler) | 07_AI_Module.md §2/§4/§5 | ✅ PHASE_19'da kodlandı (`ClaudeClient`, `AiRespondController`, `MessageLogRepository::recentForCustomer`; HMAC + service kanal, tenant_id body'den). Fallback yolları (enabled=false / anahtar yok / LLM hatası) HMAC imzalı isteklerle gerçek Postgres'e karşı test edildi. **n8n dalı PHASE_19'da bağlandı, PHASE_20'de gerçek n8n'e karşı uçtan uca çalıştırıldı:** `01_incoming...` Switch'in fallback (unhandled) çıkışı artık `Sign: AI Respond` → `AI Respond` (POST /ai/respond, neverError) → `Build AI Reply Msg` (intent='appointment_action' ise menüye yönlendirme notu ekler, 07§2 adım 6) → `Send AI Reply` (type='text' /internal/whatsapp/send) → `Respond OK (ai)` zincirine gider. **PHASE_25: sağlayıcı Gemini'ye açıldı ve gerçek anahtarla doğrulandı** (`LlmClient` arayüzü + `GeminiClient`, `.env` GEMINI_API_KEY önceliği; ücretsiz katman kararı). **(a) ✅ gerçek LLM yolu (`source:llm`) kapandı** — gerçek `GEMINI_API_KEY` ile `gemini-2.5-flash`'a karşı uçtan uca test edildi: doğru SSS yanıtı (`intent:faq`) + guardrail'ler (randevu işlemi → `appointment_action` reddi, konu dışı → red, olmayan hizmet → uydurma yok). **(b) ✅ PHASE_26'da kapandı:** `Determine Route` artık yalnızca `step==='idle'` (ilk temas) text'i `new_session`'a yolluyor; akış ortasındaki (`step!=idle`) serbest text `ai_question` route'una → Switch fallback (`unhandled`) → AI zincirine gider. Gerçek n8n v2.28.6 + Backend + Postgres + Gemini ile uçtan uca doğrulandı (mid-flow→AI text yanıtı, idle→interactive menü). Bu maddedeki tüm alt-görevler tamamlandı |
| 28 | Inbound müşteri mesajları `message_log`'a hiç yazılmıyor — `01_incoming...`'in hiçbir node'u bunu yapmıyor | 04_n8n_Workflows.md, 05_WhatsApp_Integration.md §3 | ✅ PHASE_22'de kodlandı (`WhatsAppInternalController::logInbound`, `POST /internal/whatsapp/log-inbound`, `migrations/0003` — `status` CHECK'e `'received'` eklendi; n8n `Upsert Customer`'dan sonra paralel `Sign: Log Inbound` → `Log Inbound Message` dalı). Gerçek backend + Postgres'e karşı uçtan uca doğrulandı: sıfırdan müşteri → inbound log → aynı müşteriye `type=text` gönderiminde artık `409 window_closed` dönmüyor |

## B. Ortam/Altyapı Notları (kod değil ama kodlamayı engelleyebilir)

| # | Not | Öncelik |
|---|-----|---------|
| E1 | ✅ Kapandı (PHASE_28) — kullanıcı yönetici PowerShell'de `pg_ctl register -N "postgresql-x64-16" -D "C:\Program Files\PostgreSQL\16\data"` çalıştırdı. Elle başlatılmış eski örnek (data dizinini kilitliyordu, `Start-Service` ilk denemede `OpenError` ile başarısız oldu) bu oturumdan `pg_ctl -m fast stop` ile düzgün kapatıldı, ardından `Start-Service postgresql-x64-16` başarılı oldu. **Doğrulandı:** `Get-Service postgresql-x64-16` → `Running`/`StartType: Automatic`; `postgres.exe` süreçleri artık `Services` session'ında (0) çalışıyor (eski `Console` session'ından farklı); `psql` ile `tenants` tablosuna gerçek sorgu atıldı, veri sağlam. Makine yeniden başlasa da artık elle `pg_ctl start` gerekmiyor. | ✅ Kapandı |
| E2 | ✅ Kapandı — `pdo_pgsql`/`pgsql` `C:\xampp\php\php.ini`'de etkin. **PHP sürümü PHASE_27'de kontrol edildi: 8.2.12** (karar 8.2+ idi, artık karşılanıyor — önceki oturumların 8.0.30 bulgusu bu makinede/oturumda geçerli değil, muhtemelen ara bir XAMPP güncellemesiyle değişti). Kodda hâlâ bilinçli 8.0-uyumlu yazım var ama artık zorunlu değil, zarar da vermiyor. | ✅ Kapandı |
| E3 | ✅ Çözüldü — Composer `C:\xampp\php\composer.phar` olarak kuruldu (PATH'e eklenmedi, `php composer.phar` ile çağrılıyor). `composer.json`'da hâlâ harici paket yok (`Support\Jwt` bağımlılıksız yazıldığı için ihtiyaç olmadı). | ✅ Kapandı |
| E4 | `access_token_encrypted` için tek ortak `APP_ENCRYPTION_KEY` (.env, AES-256-GCM) eklendi — KMS entegrasyonu yok (05§1 notu), tüm tenant'lar aynı anahtarı paylaşıyor. | 🟡 Orta — üretimde KMS/secret manager'a taşınmalı |
| E5 | ✅ Çözüldü — `GET /conversation-state?tenant_id=&customer_id=` bu oturumda kodlandı ve test edildi (bkz. §A madde 3, CHANGELOG PHASE_12). | ✅ Kapandı |
| E6 | ✅ Kapandı (PHASE_27, hata düzeltmesi PHASE_29) — `public/.htaccess` eklendi (mevcut dosya/dizinleri sun, geri kalanı `index.php`'ye yönlendir) + `C:\xampp\apache\conf\httpd.conf`'a `Listen 8081` + `conf\extra\httpd-vhosts.conf`'a `DocumentRoot` doğrudan `public/`'a işaret eden ayrı bir `<VirtualHost *:8081>` (mevcut port-80 `localhost` yapılandırmasına dokunulmadı). **PHASE_29 gerçek bug:** Apache mod_php `Authorization` header'ını `$_SERVER['HTTP_AUTHORIZATION']`'a iletmiyor (PHP built-in server ediyor) — panel girişi başarılı oluyor ama sonraki her JWT korumalı istek 401 dönüp anında login'e geri atıyordu. `.htaccess`'e `RewriteCond %{HTTP:Authorization} ^(.*)` + `RewriteRule ^ - [E=HTTP_AUTHORIZATION:%1]` eklenerek düzeltildi, Claude Preview'da uçtan uca doğrulandı (önce 401×5 + geri dönüş, sonra 200×5 + dashboard'da kalma). | ✅ Kapandı |
| E7 | n8n bu makineye `npm install -g n8n` ile kuruldu (v2.28.6); global npm kurulumu n8n'in kendi lockfile'ını kullanmadığından bozuk bir `@langchain/core` sürüm çakışması çıktı, elle yamalandı (1.1.41 → 1.2.1). `npm update -g n8n`/yeniden kurulumda bu yama kaybolabilir — bkz. `n8n/README.md` "n8n v2.28.6 kurulum notları". | 🟡 Orta — sonraki n8n güncellemesinde tekrar kontrol et |
| E8 | ✅ Çözüldü — kullanıcı ücretli bağımlılık istemediği için FullCalendar Scheduler (`resourceTimeGrid`, ticari lisans gerektiren) kaldırıldı, ücretsiz çekirdek pakete (`timeGridDay` + `listDay`, MIT lisans) geçildi; personel adı artık ayrı sütun yerine event title'ında gösteriliyor (PHASE_24). | ✅ Kapandı |
| E9 | ✅ Çözüldü (bu makineye özel) — Bu makinede tüm giden HTTPS istekleri (PHP curl, Node axios/undici) "unable to verify the first certificate" ile başarısız oluyordu; Windows'un kendi sertifika deposu (schannel/curl.exe) sorunsuzdu → makinede PHP'nin eski bundled `curl-ca-bundle.crt`'sinde olmayan bir sistem/kurumsal kök sertifika var. `Cert:\LocalMachine\Root`+`CA`+`CurrentUser\Root` PEM'e aktarılıp eski bundle ile birleştirildi (`C:\xampp\php\extras\ssl\combined-ca-bundle.pem`), `php.ini`'nin `curl.cainfo`/`openssl.cafile`'ına yazıldı (PHASE_20). n8n/Node tarafı için aynı sorun düzeltilmedi (yalnızca telemetri çağrılarını etkiliyor, işlevsel değil) — gerekirse `NODE_EXTRA_CA_CERTS` ile aynı PEM verilebilir. | ✅ Kapandı (bu makine) — başka bir makinede tekrar çıkarsa aynı yöntem uygulanabilir |

## C. Pilot Hata Kayıtları (08_Test_Pilot.md §4 formatı)

_Pilot henüz başlamadı — bu bölüm pilot fazında doldurulacak._

| Tarih | Senaryo # | Gözlem | Önerilen Düzeltme | Öncelik |
|-------|-----------|--------|---------------------|---------|
| — | — | — | — | — |
